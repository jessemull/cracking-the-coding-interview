const assert = require('node:assert')
const eggDropProblem = require('./practice')
const test = require('node:test')

test('eggDropProblem - []', () => {
  const output = eggDropProblem()
  assert.deepEqual(output, )
})
