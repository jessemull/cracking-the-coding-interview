const assert = require('node:assert')
const theApocalypse = require('./practice')
const test = require('node:test')

test('theApocalypse - []', () => {
  const output = theApocalypse()
  assert.deepEqual(output, )
})
