const assert = require('node:assert')
const basketball = require('./practice')
const test = require('node:test')

test('basketball - []', () => {
  const output = basketball()
  assert.deepEqual(output, )
})
