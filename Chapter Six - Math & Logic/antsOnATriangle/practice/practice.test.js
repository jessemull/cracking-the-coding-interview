const assert = require('node:assert')
const antsOnATriangle = require('./practice')
const test = require('node:test')

test('antsOnATriangle - []', () => {
  const output = antsOnATriangle()
  assert.deepEqual(output, )
})
